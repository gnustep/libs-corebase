# Apple CoreFoundation returns kCFRunLoopRunFinished here and never performs the
# signaled sources in source.m, and timers.m currently hangs instead of exiting.
export APPLE_SKIP_TESTS="source.m timers.m"
